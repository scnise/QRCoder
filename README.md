## QRCoder
For using it just download **ad.py** and **requirements.txt**. After that *pip install -r requirements.txt* to install everything and create .env file and write *TOKEN=*Your token from BotFather** and save it. **Ready!**
